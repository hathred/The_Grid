# Grok: Summarize - DML Pipeline Configuration
# Location: E:\NextGen\Offline_IT_Ecosystem\210_Project_Dev_Tools\Summarize\config.yaml
# Version: 1.0.0 (Manifest Sovereign Edition)
# Last Updated: 2025-11-02

paths:
  content_root: E:/NextGen/Offline_IT_Ecosystem
  output_root: E:/NextGen/Offline_IT_Ecosystem/210_Project_Dev_Tools/Summarize/output

file_patterns:
  - '*.json'
  - '*.yaml'
  - '*.yml'
  - '*.txt'
  - '*.md'

encoding:
  compress: true   # ← ENABLE GZIP
  
processing:
  require_manifest: true          # ONLY process dirs with DIRECTORY_MANIFEST.md
  output_mode: mirror             # mirror: preserve folder structure | centralized: flatten
  workers: 4                      # Future: parallel processing (not yet in code)

key_mappings:
  # Global minification rules (applied by cleaner_modules.py)
  name: n
  title: t
  description: d
  version: v
  path: p
  purpose: pur
  contents: c
  dependencies: dep
  setup: s
  notes: nts
  todo: td

  # MTG / Scryfall specific
  mana_cost: mc
  oracle_text: ot
  type_line: tl
  power: pw
  toughness: tg
  colors: col
  set_name: sn
  rarity: r

  # Project-specific
  project_name: pn
  project_goal: pg
  deployment_path: dp
  auto_download_url: url
  backfilled_from: bf

logging:
  level: INFO
  max_log_files: 10
  log_dir: E:/NextGen/Offline_IT_Ecosystem/995_Project_Logs

validation:
  require_purpose: true
  require_version: true
  allow_empty_contents: false

# Future extensions
# compression:
#   pre_base64: gzip          # Optional: gzip before Base64 (extra 10-15% savings)
#   level: 6